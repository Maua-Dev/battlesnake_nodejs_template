{
  "name": "battlesnake",
  "version": "1.0.0",
  "description": "Template de Battlesnake em TypeScript rodando em AWS Lambda - Dev. Community Maua",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/local.ts",
    "test": "cd dist && node --test",
    "pretest": "npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
